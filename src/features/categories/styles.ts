import styled from 'styled-components';

export const CategoriesElement = styled.div`
  border-right: var(--border-category);
  height: 100vh; 
`;

export const List = styled.ul`
  
`;

export const ListItem = styled.li`
  list-style: none;
  margin: 1rem 0;
  padding: 1rem 0;
  cursor: pointer;
  border-bottom: var(--border-category)
`;